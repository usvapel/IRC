| function call                                                                                                 | reference                                                              | notes                                                                                          | library                                                                            |
| :------------------------------------------------------------------------------------------------------------ | :--------------------------------------------------------------------- | :--------------------------------------------------------------------------------------------- | :--------------------------------------------------------------------------------- |
| `int socket(int domain, int type, int protocol)`                                                              | [manpage](https://man7.org/linux/man-pages/man2/socket.2.html)         | creates an endpoint for communication, return value is a file descriptor                       | `#include <sys/socket.h>`                                                          |
| `int close(int fd)`                                                                                           | [manpage](https://man7.org/linux/man-pages/man2/close.2.html)          | closes a file descriptor, so that it no longer refers to any file and may be reused            | `#include <unistd.h>`                                                              |
| `int setsockopt(int sockfd, int level, int optname, const void *optval, socklen_t optlen)`                    | [manpage](https://man7.org/linux/man-pages/man2/setsockopt.2.html)     | sets options on a socket (e.g., `SO_REUSEADDR`)                                                | `#include <sys/socket.h>`                                                          |
| `int getsockname(int sockfd, struct sockaddr *addr, socklen_t *addrlen)`                                      | [manpage](https://man7.org/linux/man-pages/man2/getsockname.2.html)    | returns the current address to which the socket is bound                                       | `#include <sys/socket.h>`                                                          |
| `struct protoent *getprotobyname(const char *name)`                                                           | [manpage](https://man7.org/linux/man-pages/man3/getprotobyname.3.html) | returns a protocol entry from the protocol database matching the protocol name                 | `#include <netdb.h>`                                                               |
| `struct hostent *gethostbyname(const char *name)`                                                             | [manpage](https://man7.org/linux/man-pages/man3/gethostbyname.3.html)  | retrieves host network entry (obsolete; `getaddrinfo` is preferred)                            | `#include <netdb.h>`                                                               |
| `int getaddrinfo(const char *node, const char *service, const struct addrinfo *hints, struct addrinfo **res)` | [manpage](https://man7.org/linux/man-pages/man3/getaddrinfo.3.html)    | performs network address and service translation                                               | `#include <netdb.h>`<br>`#include <sys/types.h>`<br>`#include <sys/socket.h>`      |
| `void freeaddrinfo(struct addrinfo *res)`                                                                     | [manpage](https://man7.org/linux/man-pages/man3/freeaddrinfo.3.html)   | frees the memory allocated for the dynamically allocated linked list `res`                     | `#include <netdb.h>`                                                               |
| `int bind(int sockfd, const struct sockaddr *addr, socklen_t addrlen)`                                        | [manpage](https://man7.org/linux/man-pages/man2/bind.2.html)           | assigns the address specified by `addr` to the socket referred to by `sockfd`                  | `#include <sys/socket.h>`                                                          |
| `int connect(int sockfd, const struct sockaddr *addr, socklen_t addrlen)`                                     | [manpage](https://man7.org/linux/man-pages/man2/connect.2.html)        | initiates a connection on a socket to the specified address                                    | `#include <sys/socket.h>`                                                          |
| `int listen(int sockfd, int backlog)`                                                                         | [manpage](https://man7.org/linux/man-pages/man2/listen.2.html)         | marks the socket referred to by `sockfd` as a passive socket, waiting for incoming connections | `#include <sys/socket.h>`                                                          |
| `int accept(int sockfd, struct sockaddr *addr, socklen_t *addrlen)`                                           | [manpage](https://man7.org/linux/man-pages/man2/accept.2.html)         | extracts the first connection request on the queue of pending connections, returning a new fd  | `#include <sys/socket.h>`                                                          |
| `uint16_t htons(uint16_t hostshort)`                                                                          | [manpage](https://man7.org/linux/man-pages/man3/htons.3.html)          | converts the unsigned short integer from host byte order to network byte order                 | `#include <arpa/inet.h>`                                                           |
| `uint32_t htonl(uint32_t hostlong)`                                                                           | [manpage](https://man7.org/linux/man-pages/man3/htonl.3.html)          | converts the unsigned integer from host byte order to network byte order                       | `#include <arpa/inet.h>`                                                           |
| `uint16_t ntohs(uint16_t netshort)`                                                                           | [manpage](https://man7.org/linux/man-pages/man3/ntohs.3.html)          | converts the unsigned short integer from network byte order to host byte order                 | `#include <arpa/inet.h>`                                                           |
| `uint32_t ntohl(uint32_t netlong)`                                                                            | [manpage](https://man7.org/linux/man-pages/man3/ntohl.3.html)          | converts the unsigned integer from network byte order to host byte order                       | `#include <arpa/inet.h>`                                                           |
| `in_addr_t inet_addr(const char *cp)`                                                                         | [manpage](https://man7.org/linux/man-pages/man3/inet_addr.3.html)      | converts the Internet host address `cp` from IPv4 numbers-and-dots notation into binary data   | `#include <sys/socket.h>`<br>`#include <netinet/in.h>`<br>`#include <arpa/inet.h>` |
| `char *inet_ntoa(struct in_addr in)`                                                                          | [manpage](https://man7.org/linux/man-pages/man3/inet_ntoa.3.html)      | converts the Internet host address `in`, given in network byte order, to a string              | `#include <sys/socket.h>`<br>`#include <netinet/in.h>`<br>`#include <arpa/inet.h>` |
| `const char *inet_ntop(int af, const void *src, char *dst, socklen_t size)`                                   | [manpage](https://man7.org/linux/man-pages/man3/inet_ntop.3.html)      | converts an IPv4 or IPv6 network address into a character string                               | `#include <arpa/inet.h>`                                                           |
| `ssize_t send(int sockfd, const void *buf, size_t len, int flags)`                                            | [manpage](https://man7.org/linux/man-pages/man2/send.2.html)           | transmits a message to another socket (must be in a connected state)                           | `#include <sys/socket.h>`                                                          |
| `ssize_t recv(int sockfd, void *buf, size_t len, int flags)`                                                  | [manpage](https://man7.org/linux/man-pages/man2/recv.2.html)           | receives a message from a socket                                                               | `#include <sys/socket.h>`                                                          |
| `void (*signal(int signum, void (*handler)(int)))(int)`                                                       | [manpage](https://man7.org/linux/man-pages/man2/signal.2.html)         | sets the disposition of the signal `signum` to `handler`                                       | `#include <signal.h>`                                                              |
| `int sigaction(int signum, const struct sigaction *act, struct sigaction *oldact)`                            | [manpage](https://man7.org/linux/man-pages/man2/sigaction.2.html)      | examines and/or changes the action taken by a process on receipt of a specific signal          | `#include <signal.h>`                                                              |
| `int sigemptyset(sigset_t *set)`                                                                              | [manpage](https://man7.org/linux/man-pages/man3/sigemptyset.3.html)    | initializes the signal set given by `set` to empty                                             | `#include <signal.h>`                                                              |
| `int sigfillset(sigset_t *set)`                                                                               | [manpage](https://man7.org/linux/man-pages/man3/sigfillset.3.html)     | initializes the signal set to full, including all signals                                      | `#include <signal.h>`                                                              |
| `int sigaddset(sigset_t *set, int signum)`                                                                    | [manpage](https://man7.org/linux/man-pages/man3/sigaddset.3.html)      | adds signal `signum` to `set`                                                                  | `#include <signal.h>`                                                              |
| `int sigdelset(sigset_t *set, int signum)`                                                                    | [manpage](https://man7.org/linux/man-pages/man3/sigdelset.3.html)      | deletes signal `signum` from `set`                                                             | `#include <signal.h>`                                                              |
| `int sigismember(const sigset_t *set, int signum)`                                                            | [manpage](https://man7.org/linux/man-pages/man3/sigismember.3.html)    | tests whether `signum` is a member of `set`                                                    | `#include <signal.h>`                                                              |
| `off_t lseek(int fd, off_t offset, int whence)`                                                               | [manpage](https://man7.org/linux/man-pages/man2/lseek.2.html)          | repositions the file offset of the open file description associated with the fd                | `#include <sys/types.h>`<br>`#include <unistd.h>`                                  |
| `int fstat(int fd, struct stat *statbuf)`                                                                     | [manpage](https://man7.org/linux/man-pages/man2/fstat.2.html)          | returns information about a file in the buffer pointed to by `statbuf`                         | `#include <sys/types.h>`<br>`#include <sys/stat.h>`<br>`#include <unistd.h>`       |
| `int fcntl(int fd, int cmd, ... /* arg */ )`                                                                  | [manpage](https://man7.org/linux/man-pages/man2/fcntl.2.html)          | performs one of the operations described by `cmd` on the open file descriptor `fd`             | `#include <unistd.h>`<br>`#include <fcntl.h>`                                      |
| `int poll(struct pollfd *fds, nfds_t nfds, int timeout)`                                                      | [manpage](https://man7.org/linux/man-pages/man2/poll.2.html)           | waits for one of a set of file descriptors to become ready to perform I/O                      | `#include <poll.h>`                                                                |
| `int epoll_create(int size)`                                                                                  | [manpage](https://man7.org/linux/man-pages/man7/epoll.7.html)          | creates a new `epoll(7)` instance, returning a file descriptor (represents the epoll API)      | `#include <sys/epoll.h>`                                                           |